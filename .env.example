# AWS Configuration
AWS_REGION=us-east-1          # AWS region for services
AWS_PROFILE=my-app-profile         # AWS credentials profile name

# AWS Bedrock Configuration
BEDROCK_MODEL_ID=us.anthropic.claude-3-7-sonnet-20250219-v1:0  # Required model ID

# DynamoDB Configuration
DYNAMODB_TABLE_NAME=PTORecords  # DynamoDB table name for PTO storage

# Server Configuration
PORT=3000                     # Server port number
NODE_ENV=development          # Environment (development/production)

# Optional: SSL/TLS Configuration (for production)
# SSL_KEY_PATH=path/to/key.pem
# SSL_CERT_PATH=path/to/cert.pem
